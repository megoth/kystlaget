import { style } from "@vanilla-extract/css";

export const searchStyle = style({
  alignItems: "center",
  cursor: "pointer",
  display: "flex",
  gap: "0.25em",
  textTransform: "uppercase",
  textDecoration: "none",
  fontSize: 12,
  outline: 0,
  padding: 0,
})
