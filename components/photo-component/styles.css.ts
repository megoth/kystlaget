import { style } from "@vanilla-extract/css";

export const figureStyle = style({
  margin: "1em",
});

export const captionStyle = style({
  display: "block",
  textAlign: "start",
})

export const captionAltStyle = style({
  marginRight: "0.5em",
})
